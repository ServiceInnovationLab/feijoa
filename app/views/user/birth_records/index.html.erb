<section>
  <h2>Your birth records</h2>
  <%= render 'modal_form' %>
  <% if @birth_records.any? %>
    <div class="index-cards">
      <% @birth_records.each do |record| %>
        <%= render partial: 'shared/documents/preview', locals: {
          document: record,
          actions: [
              link_to('View', user_birth_record_path(record), class: 'btn btn-secondary'),
              render('user/shares/modal_form', document: record, classes: 'btn btn-primary')
            ]
          }
        %>
      <% end %>
    </div>
  <% else %>
    <p class="text-muted font-italic">There are currently no birth records for <%= current_user.email %>.</p>
  <% end %>
</section>
