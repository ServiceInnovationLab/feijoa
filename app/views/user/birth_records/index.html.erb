<section>
  <h2>Your documents dashboard</h2>
  <p>This is where your documents are stored. Documents shown here can be for yourself, your whānau, or others. <strong>These are not proof of identity</strong> but are a legal representation of the data of your ID which is held within the New Zealand government.</p>
  <%= render 'modal_form' %>
  <% if @birth_records.any? %>
    <div class="index-cards">
      <% @birth_records.each do |record| %>
        <%= render partial: 'shared/documents/preview', locals: {
          document: record,
        actions: [
            { title: 'View', button_class: 'secondary', link: user_birth_record_path(record) },
            { title: 'Share', button_class: 'primary', link: new_user_share_path(share: { birth_record_id: record.to_param }) }
          ]
        } %>
      <% end %>
    </div>
  <% else %>
    <p class="text-muted font-italic">There are currently no birth records for <%= current_user.email %>.</p>
  <% end %>
</section>
